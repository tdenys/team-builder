<form class="mt-2" method="post" action="{{ path('personne_delete', {'id': personne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personne.id) }}" class="btn btn-danger">
    <button class="btn btn-danger">Delete</button>
</form>
